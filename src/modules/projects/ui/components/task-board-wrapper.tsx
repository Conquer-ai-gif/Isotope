'use client'

// Re-export TaskBoard from the taskboard module so it can be imported
// by project-view without a deep cross-module path.
export { TaskBoard } from '@/modules/taskboard/ui/components/task-board'
